export type VinylDocument = {
    image: string
    title: string
    description?: string
    command: boolean
}

export type GameDocument = {
    image: string
    title: string
    description?: string
    command: boolean
}

export type BookDocument = {
    image: string
    title: string
    description?: string
    command: boolean
}
