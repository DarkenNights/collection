export type VinylDocument = {
    image: string
    title: string
    description?: string
    precommand: boolean
}

export type GameDocument = {
    image: string
    title: string
    description?: string
    precommand: boolean
}

export type BookDocument = {
    image: string
    title: string
    description?: string
    precommand: boolean
}
