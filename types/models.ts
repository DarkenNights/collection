export type VinylDocument = {
    image: string
    blur: string
    title: string
    description?: string
    command: boolean
}

export type GameDocument = {
    image: string
    blur: string
    title: string
    description?: string
    command: boolean
}

export type BookDocument = {
    image: string
    blur: string
    title: string
    description?: string
    command: boolean
}
